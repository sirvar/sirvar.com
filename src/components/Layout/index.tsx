import styled from 'styled-components';

const Layout = styled.div`
  width: calc(100% - 32px);
  max-width: 768px;
  margin: 0 auto;
  padding: max(10vw, 64px) 16px 16px 16px;
`;

export default Layout;
